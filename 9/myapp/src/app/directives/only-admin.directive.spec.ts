import { OnlyAdminDirective } from './only-admin.directive';

describe('OnlyAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAdminDirective();
    expect(directive).toBeTruthy();
  });
});
